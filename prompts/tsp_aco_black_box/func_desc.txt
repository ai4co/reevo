The `heuristics` function takes as input a matrix of edge attributes and returns prior indicators of how promising it is to include each edge in a solution. The return is of the same shape as the input matrix.